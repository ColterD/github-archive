# system-domain-renamer
Tool that allows you to change a computer's device name (like on a domain especially)

Executable from the EXE or the PS1 (if you don't trust the EXE).
Pretty straightforward.

Old computer name:
New computer name:
Description (optional):

Then domain username and password if required.
Reboot optional.  Validate credentials/settings.  Save.
You're done.

# Fill when Utah CloudLab is online
# (Real values belong in ops/utah.env, which is git-ignored.)

# -----------------------------------------------------
# REMOTE ENDPOINT
# -----------------------------------------------------
# UTAH_HOST=example.yourdomain.tld
# UTAH_USER=ubuntu
# UTAH_PORT=22

# Remote base directory for sync/push/pull
# Default if unset: ~/mn-sync/cloudlab
# Note: Keep the "~" unquoted so it expands on the remote side.
# UTAH_REMOTE_BASE=~/mn-sync/cloudlab

# -----------------------------------------------------
# LOCAL PATHS (WSL)
# -----------------------------------------------------
# Default local base (current):
#   CLOUDLAB_LOCAL_BASE=/mnt/e/Services/cloudlab
#
# If you move datasets/artifacts/backups to another Windows volume (ex: Dev Drive):
#   CLOUDLAB_LOCAL_BASE=/mnt/d/Dev/cloudlab
#
# Optional per-type overrides (defaults shown):
#   CLOUDLAB_DATASETS_DIR=$CLOUDLAB_LOCAL_BASE/datasets
#   CLOUDLAB_ARTIFACTS_DIR=$CLOUDLAB_LOCAL_BASE/artifacts
#   CLOUDLAB_BACKUPS_DIR=$CLOUDLAB_LOCAL_BASE/backups

# -----------------------------------------------------
# SAFETY TOGGLES
# -----------------------------------------------------
# Enable mirror deletes (dangerous; removes files on destination not present on source)
# Used by: utah-push.sh, utah-rsync.sh (if opted in)
# UTAH_RSYNC_DELETE=1

# Dry-run + itemize changes (safe; shows what would change without doing it)
# Used by: utah-push.sh, utah-rsync.sh, utah-pull-backups.sh
# UTAH_RSYNC_DRYRUN=1
